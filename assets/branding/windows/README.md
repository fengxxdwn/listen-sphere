# Windows generated branding

`ListenSphere.ico` is generated from the approved source master with 16, 24,
32, 48, 64, 128 and 256 pixel PNG frames. Both WPF projects reference this one
file; future packaging must also reuse it instead of copying another icon.
