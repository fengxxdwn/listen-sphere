# Windows generated branding

P11-C will generate `ListenSphere.ico` here from the approved source master.
The ICO must contain at least 16, 24, 32, 48, 64, 128 and 256 pixel images and
will be referenced by both WPF projects and the Inno Setup script from this one
location. The file is intentionally absent until approved artwork is supplied.
