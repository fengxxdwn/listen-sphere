<UserControl x:Class="ListenSphere.Controller.Views.MicrophoneMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Grid.Column="2"
                Margin="8,0"
                VerticalAlignment="Center"
                IsEnabled="{Binding Network.Microphone.MicrophoneOutputEnabled}">
        <Grid>
            <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="9"
                       Text="麦克风中枢总音量" />
            <TextBlock HorizontalAlignment="Right"
                       FontSize="10"
                       Text="{Binding Network.Microphone.MicrophoneOutputVolumePercent, StringFormat={}{0:F0}%}" />
        </Grid>
        <Slider Margin="0,5,0,0"
                Minimum="0"
                Maximum="100"
                Value="{Binding Network.Microphone.MicrophoneOutputVolumePercent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <CheckBox Margin="0,5,0,0"
                  Style="{StaticResource FluentSwitchCheckBox}"
                  IsChecked="{Binding Network.Microphone.MicrophoneMonitoringEnabled, Mode=TwoWay}"
                  Content="监听到扬声器"
                  ToolTip="开启后手机麦克风会从下方选择的设备发声，可能产生回声或啸叫" />
        <ComboBox AutomationProperties.Name="麦克风监听播放设备"
                  Margin="0,5,0,0"
                  Height="28"
                  ItemsSource="{Binding Network.Microphone.PlaybackDevices}"
                  SelectedIndex="{Binding Network.Microphone.SelectedMicrophoneMonitoringDeviceIndex, Mode=TwoWay}"
                  Tag="{Binding Network.Microphone.SelectedMicrophoneMonitoringDeviceDisplayName}"
                  DisplayMemberPath="DisplayName"
                  ToolTip="选择手机麦克风的监听播放设备">
            <ComboBox.Style>
                <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Network.Microphone.MicrophoneMonitoringEnabled}" Value="True">
                            <Setter Property="IsEnabled" Value="True" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>
    </StackPanel>
</UserControl>
